# Frame packet: 02-problem

## Project inputs

- Project: C:\Users\Vinay\Documents\ThatAIPM\insideaiagents\hyperframes-book-to-skill
- Design tokens: C:\Users\Vinay\Documents\ThatAIPM\insideaiagents\hyperframes-book-to-skill\frame.md
- RULES_DIR: C:\Users\Vinay\.claude\skills\hyperframes-animation\rules

## Assigned storyboard block

## Frame 2 — The problem, mechanically

- scene: The same 400-page book dumped whole into a context-window bar that fills and overflows red, contrasted with the same book split into a compact core file plus small chapter tiles that only light up one at a time
- voiceover: "Feed an agent a four-hundred page book directly, and it burns through the context window before it even reaches your question. book-to-skill splits the book instead: one compact core file, plus per-chapter files that only load when you actually need them."
- duration: 14.099s
- transition_in: push-slide LEFT
- status: outline
- src: compositions/frames/02-problem.html
- type: pain_point
- persuasion: Concretization + Before/after contrast
- beat: recognition, mounting frustration resolving into clarity
- blueprint: kinetic-type-beats (Adapt — mechanics-comparison variant)
- focal: the context-window overflow bar, then the core-file + on-demand-chapter-tiles device
- roles: context-window bar = foreground subject in Scene 1 (fills red, overflows) · core
  file + chapter tiles = foreground subject in Scene 2 (green, orderly, on-demand) · tile
  ground = background
- sfx: whoosh, ui pop, chime
  (chapter tiles light on-demand), ui pop (core file settles)

narrativeRole: Makes Frame 1's contrast concrete and mechanical, not just a mood, shows exactly
why the naive approach overflows and exactly what book-to-skill's real two-tier structure does
instead.
keyMessage: The fix isn't magic, it's structural: one small always-loaded core, chapters that
load only on demand.

Scene 1 (0.0–5.5s): "Feed an agent a four-hundred page book directly, and it burns through the
context window before it even reaches your question." — a single tall book icon dumps whole
into a horizontal context-window capacity bar; the bar **fills rapidly** (`counting-dynamic-
scale` on the fill width) past its own edge and visibly overflows/cracks in red at the moment
"burns through" lands — Centered, bar spans ~80% width.
Scene 2 (5.5–9.5s): "book-to-skill splits the book instead: one compact core file," — hard cut
to the same book now split: a small solid "core file" tile settles center, calm green, sized
visibly small against the overflow bar's memory.
Scene 3 (9.5–13.5s): "plus per-chapter files that only load when you actually need them." —
4-5 small chapter tiles **stagger-assemble** around the core tile, each one lighting up green
one at a time then dimming back to a neutral outline (on-demand, not all-at-once) — Centered
grid, core tile slightly larger and fixed in the middle.
