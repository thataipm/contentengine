# Platform captions: book-to-skill-ai-agent-tool

Real facts only, verified live via the GitHub API this production session:
virgiliojr94/book-to-skill, 20,978 stars, MIT license, created 2026-05-01. Install
command and the core-file/chapter-file mechanism are verbatim from the repo's own
README, not estimated.

## Instagram

Your AI agent chokes on huge PDFs and burns through its context window before it
even reads your question.

There's a free skill that fixes it: book-to-skill. It splits any book into a
short core file that's always loaded, plus separate chapter files that only open
when your agent actually needs them. Up to 51x smaller than dumping the whole
thing in.

Almost 21,000 stars on GitHub, built in the last three months. One command in
your Claude Code terminal and it's running.

Comment BOOK and I'll send you the link.

#ClaudeCode #AIAgents #BuildInPublic #DevTools #AIAutomation

## LinkedIn

A problem anyone building with AI agents runs into eventually: feed a large
document straight into an agent's context window, and you pay for it twice,
once in tokens, once in the agent losing track of what actually matters.

book-to-skill (virgiliojr94, 20,978 stars on GitHub) solves this with a
structural fix rather than a bigger context window. It splits a book, PDF, or
EPUB into a compact core file the agent keeps loaded at all times, plus
per-chapter files that only get pulled in on demand. The README states up to a
51x reduction versus loading the source directly, and it installs with a single
command that works with Claude Code, GitHub Copilot CLI, and Amp.

Built in the last three months, which says something about how fast the
tooling around agent context management is moving right now.

Comment BOOK if you want the install link.

#AIEngineering #ClaudeCode #DeveloperTools #ContextWindow #BuildInPublic

## YouTube (Shorts)

**Title:** Your AI Agent Is Wasting Tokens on PDFs (Free Fix, 51x Smaller)

**Description:**
book-to-skill: a free tool that turns any book, PDF, or EPUB into something an
AI agent can actually read without drowning its context window.

How it works: instead of one giant file, you get a small core file plus
separate chapter files. The core stays loaded. Chapters only load when the
agent asks for them. Net result, per the project's own numbers: up to 51x
smaller than feeding in the raw document.

Repo: virgiliojr94/book-to-skill, almost 21,000 stars, shipped in the last
three months. Install: npx skills add virgiliojr94/book-to-skill (works with
Claude Code).

Want the link? Comment BOOK.

#Shorts #ClaudeCode #AIAgents #DevTools #BuildInPublic
