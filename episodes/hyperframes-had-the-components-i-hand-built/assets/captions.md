# Platform captions — hyperframes-had-the-components-i-hand-built

Real facts only, verified same production session: HyperFrames real GitHub stars checked live
via the GitHub API on 2026-08-13 (40,773, not a rounded/invented figure); the registry-overlap
claim (380 components, fourteen terminal card styles, the missing circular-loop device) is from
the episode's own STORYBOARD/production log, not restated here without basis.

## Instagram

I spent an entire session hand-building AI video components from scratch. Then I found a
public registry that already had 380 of them.

HyperFrames — 40,773 stars on GitHub. Fourteen terminal card styles. A count-up number. Even
the scanning-light effect I built by hand, already there.

One real gap: no circular loop diagram. So check first, don't assume the library doesn't have
it — most of the time, it already does.

This channel is switching to it, starting with this video. Follow along if you're building
your own AI systems — it's happening in public.

#AIWorkflows #BuildInPublic #ClaudeCode #DevTools #AIAutomation

## LinkedIn

I lost a full session hand-coding video components I didn't need to build.

Terminal cards. Count-up numbers. A container device. All hand-rolled, all working — and all
already sitting in HyperFrames' own public component registry, which I hadn't fully checked
first.

The numbers are real: HyperFrames is at 40,773 stars on GitHub, with fourteen terminal card
styles and a count-up component already built. Even a scanning-light effect I invented by hand
this session turned out to already exist there.

It wasn't a perfect match — the registry has no circular loop diagram, a real gap I'd have
missed if I'd assumed instead of checking. But that's the actual lesson: check the registry
before you build, not after.

I'm switching this channel's production engine to HyperFrames starting with this video. If
you're building your own AI systems and want the real, unfiltered process, I'm documenting it
as I go.

#BuildInPublic #AIEngineering #DeveloperTools #ClaudeCode #Automation

## YouTube (Shorts)

**Title:** I Wasted Hours Hand-Coding What Already Existed (HyperFrames, 40.7K Stars)

**Description:**
I hand-built AI video components for an entire session — terminal cards, count-up numbers, a
container device — before finding a public registry that already had 380 of them.

HyperFrames: 40,773 real GitHub stars, fourteen terminal card styles, a count-up number
already built, even a scanning-light effect I'd invented by hand this session. The one real
gap: no circular loop diagram in the registry.

This channel's production engine is switching to HyperFrames starting with this video —
follow along if you're building your own AI systems, it's happening in public.

#Shorts #ClaudeCode #AIAutomation #BuildInPublic #DevTools
